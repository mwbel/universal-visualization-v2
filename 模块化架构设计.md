# 可插拔模块化架构设计方案

## 🎯 设计目标

将概率统计模块设计成独立的、可插拔的模块，可以被多个父项目引用：
- **万物可视化** (main-app)
- **期末速通** (期末速通)
- 未来的其他项目

## 📐 架构设计

### 方案：共享模块目录 + 适配器模式

```
AlVisualization/
├── shared-modules/                    # 共享模块目录（新建）
│   └── probability_statistics/        # 独立的概率统计模块
│       ├── core/                      # 核心内容（与父项目无关）
│       │   ├── pages/                 # 所有可视化页面
│       │   ├── lib/                   # 库文件
│       │   ├── assets/                # 资源文件
│       │   └── data/                  # 数据文件
│       ├── adapters/                  # 适配器（适配不同父项目）
│       │   ├── main-app-adapter.js    # 万物可视化适配器
│       │   ├── exam-prep-adapter.js   # 期末速通适配器
│       │   └── standalone-adapter.js  # 独立运行适配器
│       ├── config/                    # 配置文件
│       │   └── module-config.json     # 模块配置
│       ├── index.html                 # 独立运行入口
│       ├── knowledge-tree.html        # 知识树
│       ├── concept-navigator.html     # 概念导航
│       ├── module.json                # 模块元数据
│       └── README.md                  # 模块说明
│
├── main-app/                          # 万物可视化主应用
│   └── modules/
│       └── probability_statistics/    # 引用共享模块（符号链接或配置）
│           └── module-ref.json        # 指向 shared-modules
│
└── 期末速通/
    └── modules/
        └── probability_statistics/    # 引用共享模块（符号链接或配置）
            └── module-ref.json        # 指向 shared-modules
```

## 🔧 实现方案

### 1. 模块结构

#### 核心模块 (shared-modules/probability_statistics/)
```
probability_statistics/
├── core/                              # 核心内容
│   ├── pages/                         # 54个可视化页面
│   │   ├── 01-独立性.html
│   │   ├── 02-随机事件与样本空间.html
│   │   └── ...
│   ├── lib/                           # 库文件
│   │   ├── plotly.min.js
│   │   └── tex-mml-chtml.js
│   └── knowledge/                     # 知识系统
│       ├── knowledge-tree.html
│       ├── concept-navigator.html
│       └── knowledge-data.js
│
├── adapters/                          # 适配器
│   ├── adapter-interface.js          # 适配器接口
│   ├── main-app-adapter.js           # 万物可视化适配器
│   └── exam-prep-adapter.js          # 期末速通适配器
│
├── config/
│   └── module-config.json            # 模块配置
│
├── index.html                         # 模块主页（可独立运行）
├── module.json                        # 模块元数据
└── README.md
```

#### 模块元数据 (module.json)
```json
{
  "name": "probability_statistics",
  "version": "3.0.0",
  "displayName": "概率论与数理统计",
  "description": "交互式概率统计可视化模块",
  "type": "pluggable-module",
  "author": "AlVisualization Team",
  "dependencies": {
    "plotly.js": "^2.0.0",
    "mathjax": "^3.0.0"
  },
  "exports": {
    "pages": "./core/pages",
    "knowledge": "./core/knowledge",
    "lib": "./core/lib"
  },
  "adapters": {
    "main-app": "./adapters/main-app-adapter.js",
    "exam-prep": "./adapters/exam-prep-adapter.js",
    "standalone": "./adapters/standalone-adapter.js"
  },
  "capabilities": [
    "visualization",
    "knowledge-tree",
    "concept-navigation",
    "offline-support"
  ]
}
```

### 2. 适配器设计

#### 适配器接口 (adapter-interface.js)
```javascript
class ModuleAdapter {
  constructor(config) {
    this.config = config;
  }

  // 获取导航路径（返回父项目的路径）
  getNavigationPath() {
    throw new Error('Must implement getNavigationPath()');
  }

  // 获取资源路径
  getResourcePath(resource) {
    throw new Error('Must implement getResourcePath()');
  }

  // 获取返回链接
  getBackLink() {
    throw new Error('Must implement getBackLink()');
  }

  // 初始化模块
  initialize() {
    throw new Error('Must implement initialize()');
  }
}
```

#### 万物可视化适配器 (main-app-adapter.js)
```javascript
class MainAppAdapter extends ModuleAdapter {
  getNavigationPath() {
    return '../../../index.html';
  }

  getResourcePath(resource) {
    return `../../shared-modules/probability_statistics/core/${resource}`;
  }

  getBackLink() {
    return {
      text: '返回 "万物可视化" 主页',
      url: this.getNavigationPath()
    };
  }

  initialize() {
    // 设置主应用的样式和配置
    this.loadGlobalStyles('../../lib/global-styles.css');
    this.setupBreadcrumb(['万物可视化', '数学', '概率统计']);
  }
}
```

#### 期末速通适配器 (exam-prep-adapter.js)
```javascript
class ExamPrepAdapter extends ModuleAdapter {
  getNavigationPath() {
    return '../../index.html';
  }

  getResourcePath(resource) {
    return `../../shared-modules/probability_statistics/core/${resource}`;
  }

  getBackLink() {
    return {
      text: '返回 "期末速通" 主页',
      url: this.getNavigationPath()
    };
  }

  initialize() {
    // 设置期末速通的样式和配置
    this.loadExamPrepStyles();
    this.setupQuickNav();
    this.enableExamMode();
  }
}
```

### 3. 模块引用配置

#### main-app 引用配置 (main-app/modules/probability_statistics/module-ref.json)
```json
{
  "type": "module-reference",
  "modulePath": "../../../shared-modules/probability_statistics",
  "adapter": "main-app",
  "config": {
    "theme": "main-app",
    "showBreadcrumb": true,
    "enableAnalytics": true
  }
}
```

#### 期末速通引用配置 (期末速通/modules/probability_statistics/module-ref.json)
```json
{
  "type": "module-reference",
  "modulePath": "../../shared-modules/probability_statistics",
  "adapter": "exam-prep",
  "config": {
    "theme": "exam-prep",
    "showQuickNav": true,
    "examMode": true
  }
}
```

## 🚀 实施步骤

### 第一步：创建共享模块目录
```bash
mkdir -p shared-modules/probability_statistics/{core,adapters,config}
```

### 第二步：迁移核心内容
```bash
# 将v3的内容迁移到共享模块
cp -r 期末速通/概率统计_可视化+知识树.v3/概率统计可视化/* \
      shared-modules/probability_statistics/core/pages/

# 复制知识系统
cp 期末速通/概率统计_可视化+知识树.v3/*.html \
   shared-modules/probability_statistics/core/knowledge/
```

### 第三步：创建适配器
- 编写 adapter-interface.js
- 编写 main-app-adapter.js
- 编写 exam-prep-adapter.js

### 第四步：更新父项目引用
- 在 main-app 中创建模块引用
- 在 期末速通 中创建模块引用
- 更新各自的 index.html

### 第五步：测试验证
- 测试从 main-app 访问模块
- 测试从 期末速通 访问模块
- 测试模块独立运行

## 💡 优势

### 1. 单一数据源
- 所有内容只维护一份
- 更新一次，所有项目同步

### 2. 灵活适配
- 通过适配器适配不同父项目
- 每个项目可以有不同的样式和行为

### 3. 独立运行
- 模块可以独立运行和测试
- 便于开发和调试

### 4. 易于扩展
- 添加新的父项目只需创建新适配器
- 模块功能扩展不影响父项目

### 5. 版本管理
- 模块有独立的版本号
- 父项目可以选择使用的模块版本

## 📋 迁移清单

- [ ] 创建 shared-modules 目录结构
- [ ] 迁移核心内容到 shared-modules
- [ ] 创建模块元数据文件
- [ ] 实现适配器接口
- [ ] 编写 main-app 适配器
- [ ] 编写 exam-prep 适配器
- [ ] 更新 main-app 引用
- [ ] 更新 期末速通 引用
- [ ] 测试所有引用路径
- [ ] 更新文档

## 🎯 下一步行动

1. 确认架构方案
2. 创建 shared-modules 目录
3. 开始迁移核心内容
4. 实现适配器系统
5. 更新父项目引用

---

**设计日期**: 2026年3月17日
**版本**: v1.0
