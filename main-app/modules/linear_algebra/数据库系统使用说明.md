# 线性代数可视化数据库系统

## 📊 系统简介

线性代数可视化数据库系统是一个用于管理已完成的可视化页面和概念映射的工具。它可以帮助你：

- 搜索已有的可视化概念
- 浏览所有章节和概念
- 添加新的概念到数据库
- 自动生成新的可视化页面模板
- 导出和备份数据库

## 🚀 快速开始

### 访问管理界面

在浏览器中打开：
```
http://localhost:8000/main-app/modules/linear_algebra/database-manager.html
```

## ⭐ 重要更新 (v2.0)

页面生成器已升级！现在生成的页面包含**概念特定的可视化实现**，而不是模拟数据：

- ✅ 实际的数学公式（不是占位符）
- ✅ 概念特定的参数设置
- ✅ 有意义的预设示例
- ✅ 完整的可视化代码（真实计算和绘图）
- ✅ 详细的几何解释

详见：[页面生成器增强说明.md](./页面生成器增强说明.md)

## 📚 系统功能

### 1. 概念搜索

在搜索框中输入概念名称或关键词，系统会：
- 搜索概念名称
- 搜索关键词
- 搜索描述内容
- 按相关度排序结果

**示例搜索：**
- "行列式" - 找到所有与行列式相关的概念
- "特征值" - 找到特征值分解相关内容
- "SVD" - 找到奇异值分解

### 2. 按章节浏览

系统按照教材章节组织所有概念：
- 第一章 行列式（2个概念）
- 第二章 矩阵（4个概念）
- 第三章 n维向量与线性方程组（3个概念）
- 第四章 线性空间（1个概念）
- 第五章 矩阵的对角化（5个概念）
- 第六章 实二次型（1个概念）
- 第七章 线性变换（1个概念）

点击章节标题可以展开/折叠该章节的所有概念。

### 3. 添加新概念

当你需要添加一个新的可视化概念时：

1. **填写概念信息**
   - 概念名称（必填）：如"克拉默法则"
   - 所属章节（必填）：选择对应的章节
   - 章节小节：如"1.5"
   - 概念描述（必填）：简要说明概念内容
   - 关键词：用逗号分隔，如"克拉默,线性方程组,行列式"
   - 状态：选择"待完成"或"已完成"

2. **系统自动检查**
   - 检查概念是否已存在
   - 验证必填字段
   - 生成页面路径

3. **点击"添加概念"**
   - 概念被添加到数据库
   - 统计信息自动更新
   - 章节列表自动刷新

### 4. 生成可视化页面

对于新添加的概念，可以自动生成页面模板：

1. 填写概念的基本信息
2. 点击"生成可视化页面"按钮
3. 系统会自动生成HTML页面并下载
4. 页面包含：
   - 统一的样式和布局
   - 侧边栏导航
   - **概念特定的数学公式**（不是占位符）
   - **智能参数控制**（根据概念类型定制）
   - 可视化区域
   - **有意义的预设示例**（不是简单数值）
   - **完整的可视化实现**（真实计算，不是模拟数据）
   - MathJax数学公式支持

#### 支持的概念类型

系统能够智能识别以下概念类型并生成特定实现：

- **行列式** - 显示平行四边形面积变换
- **克拉默法则** - 显示两直线交点求解
- **向量** - 显示向量及其模和角度
- **投影** - 显示向量投影和垂直分量
- **线性变换** - 显示网格变换效果
- 其他概念 - 使用通用模板（可扩展）

#### 示例：生成"克拉默法则"页面

输入概念信息：
- 概念名称：克拉默法则
- 关键词：克拉默,线性方程组,行列式

生成的页面将包含：
- 克拉默法则的实际公式：x_i = det(A_i) / det(A)
- 6个参数：a₁₁, a₁₂, a₂₁, a₂₂, b₁, b₂
- 3个预设：简单方程组、一般方程组、复杂方程组
- 完整的可视化：绘制两条直线和交点
- 实时计算：显示行列式和解

### 5. 导出数据库

**导出到控制台：**
- 点击"导出为JSON"
- 在浏览器控制台查看JSON数据

**下载数据库文件：**
- 点击"下载数据库"
- 保存为 `linear-algebra-visualization-db.json`
- 可用于备份或迁移

## 🗄️ 数据库结构

### 概念数据结构

```javascript
{
  concept: "概念名称",
  section: "章节小节",
  description: "概念描述",
  page: "pages/概念名称可视化.html",
  keywords: ["关键词1", "关键词2"],
  status: "completed" | "pending",
  chapter: 1,
  chapterName: "章节名称"
}
```

### 章节数据结构

```javascript
{
  number: 1,
  name: "行列式",
  conceptCount: 2,
  concepts: [...]
}
```

## 📊 当前数据库状态

### 统计信息
- **总章节数**：7章
- **总概念数**：17个
- **已完成**：17个
- **完成率**：100%

### 已收录概念列表

#### 第一章 行列式
1. 二阶行列式 - 平行四边形面积的几何意义
2. 三阶行列式 - 平行六面体体积的可视化

#### 第二章 矩阵
1. 矩阵运算 - 矩阵加法、乘法、转置的交互演示
2. 矩阵的初等变换 - 初等变换的矩阵表示
3. 高斯消元法 - 逐步展示矩阵化简过程
4. 矩阵的秩 - 秩的概念及其几何意义

#### 第三章 n维向量与线性方程组
1. 向量的线性关系 - 向量的线性组合、基和维数
2. 向量投影 - 投影和正交分解的可视化
3. 线性方程组 - 唯一解、无穷多解、无解的几何解释

#### 第四章 线性空间
1. 线性空间 - 线性空间的概念、基和维数

#### 第五章 矩阵的对角化
1. 特征值与特征向量 - 特征值和特征向量的几何意义
2. 正交矩阵 - 正交矩阵和QR分解的可视化
3. 旋转矩阵 - 2D和3D旋转变换
4. 奇异值分解 - 理解SVD的几何意义
5. 最小二乘法 - 数据拟合与线性回归

#### 第六章 实二次型
1. 二次型标准化 - 将二次型化为标准形的可视化过程

#### 第七章 线性变换
1. 线性变换 - 矩阵作为线性变换的几何解释

## 🔧 技术实现

### 核心类

**LinearAlgebraVisualizationDB**
- 数据库管理类
- 提供概念的增删改查
- 支持搜索和统计
- 支持导入导出

**VisualizationPageGenerator**
- 页面生成器类
- 根据概念自动生成HTML页面
- 提供统一的页面模板
- 支持不同类型的可视化

### 文件结构

```
linear_algebra/
├── database-manager.html           # 数据库管理界面
├── visualization-database.js       # 数据库核心类
├── page-generator.js              # 页面生成器
└── shared-styles.css              # 共享样式
```

## 💡 使用场景

### 场景1：查找已有可视化

**问题**：我想知道是否已经有"特征值"相关的可视化？

**操作**：
1. 打开数据库管理界面
2. 在搜索框输入"特征值"
3. 查看搜索结果

**结果**：找到"特征值与特征向量"可视化页面

### 场景2：添加新概念

**问题**：我想添加"克拉默法则"的可视化

**操作**：
1. 点击"添加新概念"
2. 填写信息：
   - 概念名称：克拉默法则
   - 所属章节：第一章 行列式
   - 章节小节：1.5
   - 描述：用行列式求解线性方程组
   - 关键词：克拉默,线性方程组,行列式
3. 点击"添加概念"

**结果**：概念被添加到数据库，统计信息更新

### 场景3：生成新页面

**问题**：我需要为"克拉默法则"创建可视化页面

**操作**：
1. 填写概念信息（同场景2）
2. 点击"生成可视化页面"
3. 下载生成的HTML文件
4. 将文件放到 `pages/` 目录
5. 在浏览器中打开查看效果

**结果**：获得一个完整的页面，包含：
- 克拉默法则的实际数学公式
- 6个参数控制（矩阵元素和常数项）
- 3个预设示例（简单、一般、复杂方程组）
- 完整的可视化实现（绘制两条直线和交点）
- 实时计算和结果显示

**注意**：生成的页面已经是可用的，不需要手动添加可视化逻辑！

## 🎯 最佳实践

### 1. 概念命名规范
- 使用简洁明确的名称
- 避免过长的名称
- 与教材保持一致

### 2. 关键词设置
- 包含概念的核心术语
- 包含相关的数学术语
- 包含常见的搜索词

### 3. 描述编写
- 简明扼要，一两句话说清楚
- 突出几何意义或应用价值
- 避免过于专业的术语

### 4. 页面生成后
- 检查页面结构是否正确
- 测试所有参数控制是否工作
- 尝试所有预设示例
- 验证可视化是否正确显示
- 检查数学公式渲染
- 如果是已实现的概念类型（行列式、克拉默法则、向量、投影、线性变换），页面应该可以直接使用
- 如果是其他概念类型，可能需要手动添加特定的可视化逻辑

### 5. 扩展新概念类型
如果需要为新的概念类型添加特定实现：
1. 在 `page-generator.js` 中添加检测规则（`detectConceptType` 方法）
2. 创建新的配置方法（如 `getNewConceptConfig()`）
3. 定义公式、参数、预设和可视化代码
4. 在 `getConceptConfig` 中注册新类型

## 🔄 数据库维护

### 定期备份
建议定期导出数据库：
```
点击"下载数据库" → 保存JSON文件 → 存档备份
```

### 数据同步
如果有多人协作：
1. 导出当前数据库
2. 合并其他人的更新
3. 导入合并后的数据库

### 版本管理
在数据库JSON中包含版本信息：
```json
{
  "metadata": {
    "version": "3.0",
    "lastUpdated": "2025-03-17T..."
  }
}
```

## 📞 技术支持

如有问题或建议，请查看：
- `README.md` - 项目文档
- `完善总结-v3.md` - 最新更新说明
- `页面生成器增强说明.md` - 页面生成器详细文档

---

**创建日期**：2025-03-17
**版本**：v2.0（页面生成器已升级）
**作者**：AI Assistant (Claude)
