# AI Visual Astronomy

本项目用于天文学知识的可视化，例如地球公转、月球运动、星空三维展示。

## 项目结构
- `src/` 存放源代码
- `assets/` 存放生成的动图或图片
- `requirements.txt` Python 依赖
- `venv/` 虚拟环境 (不会上传)

## 安装
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
