# 💰 关于 AI 接入费用的说明与方案

接入真实的 AI 模型确实可能产生费用，具体取决于您选择的方案。为了让项目灵活可控，我为您设计了三种模式：

## 方案 A：商业 API (推荐 DeepSeek) - **极低成本**
目前性价比最高的选择，非常适合开发测试。
- **费用**: 约 1元人民币 / 500次可视化生成。
- **优点**: 速度快，无需高性能电脑，生成代码质量极高。
- **缺点**: 需要注册并获取 API Key。
- **适用**: 绝大多数开发场景。

## 方案 B：本地模型 (Ollama) - **完全免费** 🆓
利用您自己的电脑显卡运行 AI (如 Qwen2.5-Coder 或 Llama3)。
- **费用**: 0元。
- **优点**: 隐私安全，离线可用。
- **缺点**: 需要电脑配置较好（推荐 M1/M2/M3 Mac 或 NVIDIA 显卡），运行速度可能较慢。
- **适用**: 极客玩家，或对数据隐私敏感。

## 方案 C：Mock 模式 (当前状态) - **完全免费** 🆓
使用预设的假数据模拟 AI 响应。
- **优点**: 零成本，极速响应，适合调试前端界面。
- **缺点**: 只能生成预设的几种图表（如正弦波、散点图），无法真正理解复杂指令。

---

## 🚀 我的建议
我们采用 **"兼容模式"** 开发：
1. 代码层实现标准的 OpenAI 接口调用逻辑（DeepSeek 和 Ollama 都兼容此格式）。
2. 默认情况下，如果检测不到 API Key，就回退到 **Mock 模式**。
3. 这样您**不需要现在就付费**，可以先跑通流程，未来随时填入 Key 或启动本地模型即可升级为"真智能"。

我现在就为您更新 `ApiClient.js`，使其支持这种**混合切换**能力。
