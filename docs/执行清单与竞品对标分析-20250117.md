# 执行清单与竞品对标分析
**日期**: 2025-01-17
**项目**: 万物可视化
**文档版本**: v1.0

---

## 📋 文档说明

本文档将《万物可视化执行清单20251207.md》中的任务与竞品（DeepTutor、Google Dynamic View）进行对标分析，明确哪些功能需要**超越竞品**，哪些可以**借鉴学习**，从而制定更精准的实施策略。

---

## 🎯 对标分析框架

### 对标维度
1. **功能对比**：竞品是否有类似功能？
2. **实现方式**：竞品如何实现？我们的方案有何优劣？
3. **优先级调整**：根据竞品情况调整任务优先级
4. **差异化策略**：如何在竞品基础上做出创新

### 对标符号说明
- 🔴 **竞争焦点**：竞品已实现，我们需要做得更好
- 🟡 **借鉴学习**：竞品有优势，我们可学习
- 🟢 **独特优势**：竞品没有，是我们的差异化亮点
- ⚪ **基础功能**：业界标准，必须实现

---

## 📊 详细对标分析

---

## 第一部分：现代 AI 界面 (Modern AI Interface)

### 1.1 流式对话 UI 重构

#### 任务分解
- [ ] 实现打字机效果的流式文本渲染 (Markdown + LaTeX)
- [ ] 状态指示器升级 (思考中、生成代码中、渲染中)

#### 竞品对标

| 功能点 | DeepTutor | Dynamic View | 万物可视化 | 对标结论 |
|--------|-----------|--------------|------------|---------|
| **打字机效果** | ✅ 有 | ✅ 有 | ⏳ 待开发 | ⚪ 基础功能，必须实现 |
| **Markdown 渲染** | ✅ 有 | ✅ 有 | ⏳ 待开发 | ⚪ 基础功能，必须实现 |
| **LaTeX 公式** | ✅ 有 | ✅ 有 | ⏳ 待开发 | ⚪ 基础功能，必须实现 |
| **状态指示器** | ⭐⭐⭐ 简单 | ⭐⭐⭐⭐⭐ 详细 | ⏳ 待开发 | 🔴 需要对标 Dynamic View |

#### 差异化建议
```javascript
// 借鉴 Dynamic View 的详细状态指示
const statusIndicators = {
  thinking: {
    icon: '🧠',
    text: '正在理解问题...',
    animation: 'pulse'
  },
  generating: {
    icon: '💻',
    text: '正在生成可视化代码...',
    animation: 'typing',
    progress: true  // 显示生成进度
  },
  rendering: {
    icon: '🎨',
    text: '正在渲染可视化...',
    animation: 'spin'
  },
  optimizing: {
    icon: '⚡',
    text: '正在优化性能...',
    animation: 'flash'
  }
};
```

#### 优先级
**🔴 高优先级** - Dynamic View 已做得很好，这是用户体验的关键

---

### 1.2 可视化工件 (Artifacts) 容器

#### 任务分解
- [ ] 独立的可视化面板（右侧分栏或弹窗）
- [ ] 支持代码查看/编辑模式
- [ ] 全屏/沉浸式预览模式

#### 竞品对标

| 功能点 | DeepTutor | Dynamic View | 万物可视化 | 对标结论 |
|--------|-----------|--------------|------------|---------|
| **独立面板** | ❌ 无 | ✅ 右侧 Artifact | ⏳ 待开发 | 🔴 必须对标 Dynamic View |
| **代码编辑** | ⭐⭐⭐ 有限 | ⭐⭐⭐⭐ 完整 | ⏳ 待开发 | 🔴 需要达到 Dynamic View 水平 |
| **全屏预览** | ❌ 无 | ✅ 有 | ⏳ 待开发 | ⚪ 基础功能 |
| **响应式布局** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⏳ 待开发 | 🟡 学习 Dynamic View 的布局 |

#### 差异化建议
```javascript
// 在 Dynamic View 基础上增加学科特性
class ArtifactContainer {
  constructor() {
    this.layout = 'split-right';  // 默认右侧分栏
    this.modes = ['preview', 'code', 'split'];
  }

  // 差异化：针对 STEM 优化的布局
  applySTEMOptimizations() {
    // 数学公式优化渲染
    if (this.hasMathFormulas()) {
      this.enableMathJaxAsync();
    }

    // 3D 场景自动全屏提示
    if (this.is3DScene()) {
      this.suggestFullscreen();
    }

    // 代码高亮增强（支持 SymPy、Python 等）
    this.enhanceCodeHighlighting(['python', 'sympy', 'javascript']);
  }
}
```

#### 优先级
**🔴 最高优先级** - 这是与 Dynamic View 竞争的核心界面

---

### 1.3 交互式参数面板

#### 任务分解
- [ ] AI 自动识别变量，动态生成滑块/输入框

#### 竞品对标

| 功能点 | DeepTutor | Dynamic View | 万物可视化 | 对标结论 |
|--------|-----------|--------------|------------|---------|
| **自动参数识别** | ⚠️ 有限 | ✅ 有 | ⏳ 待开发 | 🔴 竞争焦点 |
| **动态控件生成** | ⚠️ 手动 | ✅ 自动 | ⏳ 待开发 | 🔴 必须实现 |
| **参数类型推断** | ❌ 无 | ✅ 有 | ⏳ 待开发 | 🟢 我们可以做更智能 |

#### 差异化策略
```javascript
// 超越竞品：更智能的参数推断
class ParameterInference {
  inferParameters(code, subject) {
    const baseParams = this.extractBaseParams(code);

    // 差异化：根据学科增强参数
    const enhancedParams = this.enhanceForSubject(baseParams, subject);

    switch(subject) {
      case 'calculus':
        return this.addCalculusParams(enhancedParams);
      case 'physics':
        return this.addPhysicsParams(enhancedParams);
      case 'astronomy':
        return this.addAstronomyParams(enhancedParams);
      default:
        return enhancedParams;
    }
  }

  // 差异化示例：微积分参数
  addCalculusParams(params) {
    return {
      ...params,
      // 自动添加数学精度控制
      precision: {
        type: 'slider',
        min: 1,
        max: 100,
        default: 50,
        label: '采样点数（精度）',
        category: 'mathematical'
      },
      // 自动添加动画控制
      animationSpeed: {
        type: 'slider',
        min: 0.1,
        max: 2,
        default: 1,
        label: '动画速度',
        category: 'animation'
      }
    };
  }
}
```

#### 优先级
**🟡 中高优先级** - Dynamic View 有优势，但我们可以做得更智能

---

### 1.4 基础交互体验优化

#### 任务分解
- [ ] 对话布局调整（用户靠右，AI 靠左）
- [ ] 文本操作增强（选择、复制、粘贴）

#### 竞品对标

| 功能点 | DeepTutor | Dynamic View | 万物可视化 | 对标结论 |
|--------|-----------|--------------|------------|---------|
| **对话布局** | ⭐⭐⭐ 标准 | ⭐⭐⭐⭐⭐ 优化 | ⏳ 需改进 | ⚪ 业界标准 |
| **文本选择** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⏳ 待优化 | ⚪ 必须修复 |
| **复制粘贴** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⏳ 有问题 | ⚪ 必须修复 |

#### 实施建议
```css
/* 对标 Dynamic View 的用户体验 */
.message-content {
  user-select: text;           /* 允许选择 */
  -webkit-user-select: text;
  cursor: text;
}

.message-content code {
  cursor: pointer;             /* 代码块点击复制 */
  position: relative;
}

.message-content code::after {
  content: '复制';
  position: absolute;
  top: 5px;
  right: 5px;
  opacity: 0;
  transition: opacity 0.2s;
}

.message-content code:hover::after {
  opacity: 1;
}
```

#### 优先级
**🟡 中优先级** - 基础体验，必须达到业界标准

---

## 第二部分：学科路由器 (Subject Router)

### 2.1 意图识别层

#### 任务分解
- [ ] 开发 LLM 分类器（区分：闲聊/绘图/数学推导/物理模拟/天文演示）
- [ ] 提取关键实体（公式、初始条件、天体名称）

#### 竞品对标

| 功能点 | DeepTutor | Dynamic View | 万物可视化 | 对标结论 |
|--------|-----------|--------------|------------|---------|
| **意图分类** | ⭐⭐⭐ 学科分类 | ⭐⭐ 通用理解 | ⏳ 待开发 | 🟢 我们的特色功能 |
| **实体提取** | ⭐⭐⭐ | ⭐⭐ | ⏳ 待开发 | 🟢 针对STEM优化 |
| **学科路由** | ❌ 无 | ⚠️ 隐式 | ⏳ 待开发 | 🟢 核心差异化 |

#### 差异化优势
```javascript
// 独特的学科路由器（竞品都没有）
class SubjectRouter {
  constructor() {
    this.classifiers = {
      math: new MathClassifier(),
      physics: new PhysicsClassifier(),
      astronomy: new AstronomyClassifier(),
      general: new GeneralClassifier()
    };
  }

  async route(userInput) {
    // DeepTutor 和 Dynamic View 都没有显式的路由层
    // 这是我们的核心差异化

    const features = await this.extractFeatures(userInput);

    // 多级分类
    const subject = await this.classifySubject(features);
    const subfield = await this.classifySubfield(subject, features);
    const taskType = await this.classifyTaskType(subfield, features);

    // 路由到专属引擎
    return this.dispatchToEngine(subject, subfield, taskType);
  }

  // 差异化：细粒度的学科分类
  async classifySubfield(subject, features) {
    const subfields = {
      math: ['calculus', 'algebra', 'geometry', 'statistics'],
      physics: ['mechanics', 'electromagnetism', 'thermodynamics', 'quantum'],
      astronomy: ['celestial', 'astrophysics', 'observation']
    };

    // 使用专门的分类器
    return this.classifiers[subject].classifySubfield(features);
  }
}
```

#### 优先级
**🟢 最高优先级** - 这是我们的核心差异化，竞品都没有

---

### 2.2 路由策略

#### 任务分解
- [ ] 数学路由：指向 Python/SymPy + Plotly
- [ ] 物理路由：指向 Matter.js / Three.js
- [ ] 通用/统计路由：指向 ECharts / Plotly

#### 竞品对标

| 功能点 | DeepTutor | Dynamic View | 万物可视化 | 对标结论 |
|--------|-----------|--------------|------------|---------|
| **智能路由** | ❌ 无 | ⚠️ 隐式 | ⏳ 待开发 | 🟢 独特优势 |
| **技术栈选择** | ⚠️ 固定 | ⚠️ 通用 | ⏳ 灵活 | 🟢 独特优势 |
| **优化策略** | ❌ 无 | ❌ 无 | ⏳ 待开发 | 🟢 独特优势 |

#### 差异化优势
```javascript
// 竞品都没有的显式路由策略
const ROUTING_STRATEGIES = {
  calculus: {
    primary: 'sympy+plotly',
    fallback: 'matplotlib',
    optimizations: [
      'use_mathjax_for_formulas',
      'animate_derivatives',
      'show_riemann_sums'
    ]
  },

  mechanics: {
    primary: 'matterjs',
    fallback: 'threejs',
    optimizations: [
      'real_time_physics',
      'energy_tracking',
      'force_visualization'
    ]
  },

  astronomy: {
    primary: 'threejs+astronomy',
    fallback: 'plotly_3d',
    optimizations: [
      'realistic_scales',
      'orbital_mechanics',
      'time_compression'
    ]
  }
};
```

#### 优先级
**🟢 最高优先级** - 核心差异化功能

---

### 2.3 上下文管理

#### 任务分解
- [ ] 维护多轮对话状态
- [ ] 支持"把这个球变红"等修改指令

#### 竞品对标

| 功能点 | DeepTutor | Dynamic View | 万物可视化 | 对标结论 |
|--------|-----------|--------------|------------|---------|
| **多轮对话** | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⏳ 待开发 | 🔴 超越 DeepTutor |
| **修改指令** | ⭐⭐⭐ | ⭐⭐ | ⏳ 待开发 | 🔴 超越竞品 |
| **状态保持** | ⭐⭐⭐ | ⭐⭐ | ⏳ 待开发 | 🟡 学习 DeepTutor |

#### 差异化建议
```javascript
// 在 DeepTutor 基础上增强
class ContextManager {
  constructor() {
    this.conversationHistory = [];
    this.currentVisualization = null;
    this.modificationHistory = [];
  }

  // 借鉴 DeepTutor 的上下文理解
  async handleModification(userInput) {
    const intent = await this.parseModificationIntent(userInput);

    // 差异化：更智能的参数修改
    switch(intent.type) {
      case 'parameter_change':
        return this.modifyParameter(intent);
      case 'visual_change':
        return this.modifyVisual(intent);
      case 'add_element':
        return this.addElement(intent);
      case 'remove_element':
        return this.removeElement(intent);
    }
  }

  // 示例：智能参数修改
  async modifyParameter(intent) {
    // "把重力调大一点"
    if (intent.natural === '把重力调大一点') {
      const current = this.currentVisualization.params.gravity;
      const newValue = this.applyIncrement(current, intent.modifier);

      return {
        param: 'gravity',
        value: newValue,
        explanation: `重力从 ${current} 增加到 ${newValue}`
      };
    }
  }
}
```

#### 优先级
**🔴 高优先级** - DeepTutor 有优势，我们需要对标并超越

---

## 第三部分：通用可视化引擎 (General Visualization Engine)

### 3.1 动态代码生成器

#### 任务分解
- [ ] 设计通用的 System Prompt
- [ ] 注入标准库支持（Plotly, Three.js, MathJax等）

#### 竞品对标

| 功能点 | DeepTutor | Dynamic View | 万物可视化 | 对标结论 |
|--------|-----------|--------------|------------|---------|
| **代码生成** | ✅ 有 | ✅ 有 | ⏳ 待开发 | ⚪ 基础功能 |
| **Prompt 模板** | ⚠️ 固定 | ⚠️ 通用 | ⏳ 待优化 | 🟢 我们可以做学科专属 |
| **库注入** | ⚠️ 有限 | ✅ 丰富 | ⏳ 待开发 | 🟡 学习 Dynamic View |

#### 差异化建议
```javascript
// 学科专属 Prompt 模板（竞品都是通用模板）
const SUBJECT_PROMPTS = {
  calculus: {
    system: `你是微积分可视化专家。使用 SymPy 计算和 Plotly 渲染。
必选库：MathJax（公式）、Plotly（图表）
风格：数学严谨、动画流畅、交互丰富
`,
    examples: [...]  // 微积分专属示例
  },

  mechanics: {
    system: `你是物理仿真专家。使用 Matter.js 建立物理世界。
必选库：Matter.js（物理引擎）、Three.js（3D）
风格：真实物理、能量守恒可视化
`,
    examples: [...]  // 物理专属示例
  }
};
```

#### 优先级
**🔴 高优先级** - 核心生成能力

---

### 3.2 安全沙箱环境

#### 任务分解
- [ ] 升级 `<iframe>` 机制
- [ ] 实现父子页面通信（postMessage）

#### 竞品对标

| 功能点 | DeepTutor | Dynamic View | 万物可视化 | 对标结论 |
|--------|-----------|--------------|------------|---------|
| **沙箱隔离** | ✅ 有 | ✅ 有 | ⏳ 待开发 | ⚪ 基础功能 |
| **通信机制** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⏳ 待开发 | 🟡 学习 Dynamic View |
| **安全策略** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⏳ 待开发 | 🔴 必须严格 |

#### 实施建议
```javascript
// 对标 Dynamic View 的安全策略
class SandboxConfig {
  static getDefaultConfig() {
    return {
      sandbox: [
        'allow-scripts',
        'allow-same-origin',
        'allow-forms'
        // 注意：不使用 'allow-top-navigation'
      ],
      permissions: {
        // 仅允许必要的 API
        allowedAPIs: ['requestAnimationFrame', 'setTimeout'],
        blockedAPIs: ['fetch', 'XMLHttpRequest', 'WebSocket']
      },
      csp: "default-src 'self'; script-src 'self' 'unsafe-inline' cdn.plot.ly cdn.jsdelivr.net"
    };
  }
}
```

#### 优先级
**🔴 最高优先级** - 安全是底线

---

### 3.3 自我修正机制

#### 任务分解
- [ ] 捕获 iframe 运行时的 JS 错误
- [ ] 将错误堆栈回传给 LLM，触发自动修复

#### 竞品对标

| 功能点 | DeepTutor | Dynamic View | 万物可视化 | 对标结论 |
|--------|-----------|--------------|------------|---------|
| **错误捕获** | ❌ 无 | ❌ 无 | ⏳ 待开发 | 🟢 独特优势 |
| **自动修复** | ❌ 无 | ❌ 无 | ⏳ 待开发 | 🟢 独特优势 |
| **降级方案** | ⚠️ 有限 | ❌ 无 | ⏳ 待开发 | 🟢 独特优势 |

#### 差异化优势
```javascript
// 竞品都没有的自我修正机制
class SelfCorrection {
  async handleError(error, originalCode, userRequest) {
    // 1. 错误分类
    const errorType = this.classifyError(error);

    // 2. 生成修复 Prompt
    const fixPrompt = `
生成的代码在运行时出错：
【错误类型】${errorType}
【错误信息】${error.message}
【堆栈】${error.stack}

【原始代码】
${originalCode}

【用户需求】
${userRequest}

请分析错误原因并修复代码。确保：
1. 修复所有错误
2. 添加必要的错误处理
3. 优化性能
4. 保持原有功能
    `;

    // 3. LLM 修复
    const fixedCode = await this.llm.generate(fixPrompt);

    // 4. 验证修复
    const isValid = await this.validate(fixedCode);

    if (isValid) {
      return fixedCode;
    } else {
      // 降级方案：生成简化版本
      return this.generateFallback(userRequest);
    }
  }
}
```

#### 优先级
**🟢 中高优先级** - 独特优势，提升稳定性

---

## 第四部分：后端与模型集成 (Infrastructure)

### 4.1 模型接入层

#### 任务分解
- [ ] 接入主流大模型 API（DeepSeek / Claude / GPT-4o）
- [ ] 建立 Prompt 模板库管理系统

#### 竞品对标

| 功能点 | DeepTutor | Dynamic View | 万物可视化 | 对标结论 |
|--------|-----------|--------------|------------|---------|
| **LLM 支持** | ✅ 多模型 | ⚠️ 仅 Gemini | ⏳ 待开发 | 🟢 更灵活 |
| **本地模型** | ⚠️ 有限 | ❌ 无 | ⏳ 待开发 | 🟢 独特优势 |
| **Prompt 管理** | ⭐⭐⭐ | ⭐⭐ | ⏳ 待开发 | 🟢 可做成社区 |

#### 差异化建议
```javascript
// 支持本地模型（竞品都没有）
class ModelManager {
  async initializeModel(config) {
    if (config.type === 'local') {
      // DeepSeek 本地部署
      return await this.loadLocalModel('deepseek-coder');
    } else if (config.type === 'api') {
      // API 调用（支持多厂商）
      const providers = {
        'deepseek': DeepSeekAPI,
        'claude': ClaudeAPI,
        'gpt-4': OpenAIAPI
      };
      return new providers[config.provider]();
    }
  }
}
```

#### 优先级
**🟡 中优先级** - 后端支持，可根据需求调整

---

### 4.2 数据与存储

#### 任务分解
- [ ] 存储用户生成的优质可视化代码
- [ ] 用户偏好设置同步

#### 竞品对标

| 功能点 | DeepTutor | Dynamic View | 万物可视化 | 对标结论 |
|--------|-----------|--------------|------------|---------|
| **代码存储** | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⏳ 待开发 | 🟡 学习 Dynamic View |
| **社区分享** | ⭐⭐ | ❌ 无 | ⏳ 待开发 | 🟢 独特优势 |
| **偏好同步** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⏳ 待开发 | ⚪ 基础功能 |

#### 优先级
**🟡 中低优先级** - 用户体验增强，可后期优化

---

## 📊 总结与建议

### 竞争态势分析

#### 🔴 竞争焦点（必须对标）
1. **Artifact UI** - Dynamic View 已经做得很好
2. **流式输出体验** - 业界标准
3. **多轮对话上下文** - DeepTutor 有优势

#### 🟢 独特优势（我们的差异化）
1. **学科路由器** - 竞品都没有的显式路由
2. **自我修正机制** - 独特的错误自动修复
3. **学科专属 Prompt** - 针对STEM深度优化
4. **逐步推导模式** - 比DeepTutor更强
5. **本地部署支持** - 竞品都没有

#### ⚪ 基础功能（业界标准）
1. **代码生成能力**
2. **沙箱安全机制**
3. **用户交互体验**

---

### 🎯 优先级调整建议

#### P0 - 立即执行（1-2个月）
1. ✅ **学科路由器** - 核心差异化，必须先做
2. ✅ **自我修正机制** - 提升稳定性关键
3. ✅ **Artifact UI** - 对标Dynamic View，用户界面基础

#### P1 - 近期执行（2-4个月）
1. 📐 **数学可视化引擎** - 深度优化微积分/线性代数
2. ⚛️ **物理可视化引擎** - 集成Matter.js + Three.js
3. 🧠 **上下文管理** - 超越DeepTutor的多轮对话

#### P2 - 中期规划（4-6个月）
1. 🌌 **天文可视化引擎** - 完善STEM覆盖
2. 📚 **逐步推导模式** - 教学增强
3. 🔧 **插件系统框架** - 生态建设

---

### 💡 实施策略

#### 策略1：差异化突破
```
不要在通用领域和Dynamic View硬碰硬
→ 专注STEM教育的深度可视化
→ 学科路由器是核心武器
```

#### 策略2：快速迭代
```
先实现核心差异化功能（学科路由器）
再对标完善基础体验（UI/UX）
最后构建生态（插件、社区）
```

#### 策略3：开源共建
```
开源Prompt模板库
社区贡献插件
本地部署方案
→ 建立护城河
```

---

## 📈 预期成果

### 短期（3个月）
- ✅ 学科路由器上线
- ✅ 超过DeepTutor的数学可视化能力
- ✅ 对标Dynamic View的UI体验

### 中期（6个月）
- ✅ 完整的STEM三大引擎（数/理/天）
- ✅ 自我修正机制成熟
- ✅ 社区开始贡献插件

### 长期（12个月）
- ✅ 成为STEM教育可视化标杆
- ✅ GitHub Stars > 1000
- ✅ 被10+高校采用

---

## 🔗 相关文档

- 执行清单：`万物可视化执行清单20251207.md`
- 差异化设计：`万物可视化-差异化功能设计-20250117.md`
- 竞品分析：`竞品分析-DeepTutor与GoogleDynamicView-20250117.md`

---

**文档生成时间**: 2025-01-17
**作者**: AI Assistant (Claude)
**项目**: 万物可视化
**版本**: v1.0
