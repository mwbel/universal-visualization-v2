# 开发工具使用指南

## 🎯 工具概览

作为开发者，掌握合适的工具能极大提高效率。以下是万物可视化项目开发中的核心工具：

## 🐍 1. Python开发工具

### 1.1 虚拟环境管理
```bash
# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境 (macOS/Linux)
source venv/bin/activate

# 激活虚拟环境 (Windows)
venv\Scripts\activate

# 退出虚拟环境
deactivate

# 安装依赖包
pip install fastapi uvicorn

# 生成依赖文件
pip freeze > requirements.txt

# 从依赖文件安装
pip install -r requirements.txt
```

### 1.2 代码质量工具

#### Black - 代码格式化
```bash
# 安装
pip install black

# 格式化单个文件
black main.py

# 格式化整个项目
black .

# 检查格式（不修改文件）
black --check .
```

#### Flake8 - 代码检查
```bash
# 安装
pip install flake8

# 检查代码
flake8 main.py

# 检查整个项目
flake8 .

# 配置文件 (.flake8)
[flake8]
max-line-length = 88
ignore = E203, W503
exclude = venv,__pycache__
```

#### MyPy - 类型检查
```bash
# 安装
pip install mypy

# 类型检查
mypy main.py

# 配置文件 (mypy.ini)
[mypy]
python_version = 3.11
warn_return_any = True
warn_unused_configs = True
disallow_untyped_defs = True
```

### 1.3 测试工具

#### pytest - 测试框架
```bash
# 安装
pip install pytest pytest-asyncio

# 运行测试
pytest

# 运行特定测试文件
pytest test_main.py

# 显示覆盖率
pytest --cov=. --cov-report=html
```

#### 测试示例 (test_main.py)
```python
import pytest
from fastapi.testclient import TestClient
from main import app

client = TestClient(app)

def test_root_endpoint():
    response = client.get("/")
    assert response.status_code == 200
    assert "name" in response.json()

def test_generate_visualization():
    response = client.post(
        "/api/v2/generate",
        json={
            "prompt": "测试正弦函数",
            "template_id": "default"
        }
    )
    assert response.status_code == 200
    assert "generation_id" in response.json()
```

## 🌐 2. 前端开发工具

### 2.1 Node.js和npm
```bash
# 检查Node.js版本
node --version

# 检查npm版本
npm --version

# 初始化项目
npm init -y

# 安装依赖
npm install lodash

# 安装开发依赖
npm install --save-dev jest

# 运行脚本
npm run test
npm run build
```

### 2.2 代码打包工具

#### Webpack配置示例
```javascript
// webpack.config.js
const path = require('path');

module.exports = {
  entry: './src/index.js',
  output: {
    filename: 'bundle.js',
    path: path.resolve(__dirname, 'dist'),
  },
  module: {
    rules: [
      {
        test: /\.js$/,
        exclude: /node_modules/,
        use: {
          loader: 'babel-loader',
        },
      },
    ],
  },
};
```

#### Vite配置（推荐）
```typescript
// vite.config.ts
import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';

export default defineConfig({
  plugins: [vue()],
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
      },
    },
  },
});
```

## 🔧 3. 版本控制 - Git

### 3.1 基础命令
```bash
# 初始化仓库
git init

# 配置用户信息
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# 查看状态
git status

# 添加文件到暂存区
git add .
git add main.py

# 提交更改
git commit -m "feat: 添加可视化生成功能"

# 查看提交历史
git log --oneline

# 查看文件差异
git diff
git diff --staged
```

### 3.2 分支管理
```bash
# 创建分支
git branch feature/new-template

# 切换分支
git checkout feature/new-template

# 创建并切换分支
git checkout -b feature/new-template

# 合并分支
git checkout main
git merge feature/new-template

# 删除分支
git branch -d feature/new-template
```

### 3.3 远程仓库
```bash
# 添加远程仓库
git remote add origin https://github.com/username/repo.git

# 推送到远程
git push -u origin main

# 拉取远程更改
git pull origin main

# 克隆仓库
git clone https://github.com/username/repo.git
```

## 🐳 4. Docker容器化

### 4.1 Dockerfile示例
```dockerfile
# backend/Dockerfile
FROM python:3.11-slim

WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    gcc \
    && rm -rf /var/lib/apt/lists/*

# 复制依赖文件
COPY requirements.txt .

# 安装Python依赖
RUN pip install --no-cache-dir -r requirements.txt

# 复制应用代码
COPY . .

# 暴露端口
EXPOSE 8000

# 启动命令
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### 4.2 Docker Compose
```yaml
# docker-compose.yml
version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://postgres:password@db:5432/app
    depends_on:
      - db

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

  db:
    image: postgres:15
    environment:
      - POSTGRES_DB=app
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
```

### 4.3 Docker命令
```bash
# 构建镜像
docker build -t myapp .

# 运行容器
docker run -p 8000:8000 myapp

# 查看运行的容器
docker ps

# 查看容器日志
docker logs container_id

# 进入容器
docker exec -it container_id bash

# 停止容器
docker stop container_id

# 删除容器
docker rm container_id

# Docker Compose
docker-compose up -d
docker-compose logs -f
docker-compose down
```

## 📊 5. 监控和调试工具

### 5.1 日志管理
```python
import logging
import sys
from datetime import datetime

# 配置日志
def setup_logging():
    logging.basicConfig(
        level=logging.INFO,
        format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
        handlers=[
            logging.FileHandler(f'app_{datetime.now().strftime("%Y%m%d")}.log'),
            logging.StreamHandler(sys.stdout)
        ]
    )

# 使用日志
logger = logging.getLogger(__name__)

@app.post("/api/v2/generate")
async def generate(request: UniversalVisualizationRequest):
    logger.info(f"收到生成请求: {request.prompt}")

    try:
        result = await process_request(request)
        logger.info(f"生成成功: {result.generation_id}")
        return result
    except Exception as e:
        logger.error(f"生成失败: {str(e)}")
        raise
```

### 5.2 性能监控
```python
import time
from functools import wraps

def timing_decorator(func):
    @wraps(func)
    async def wrapper(*args, **kwargs):
        start_time = time.time()
        try:
            result = await func(*args, **kwargs)
            end_time = time.time()
            logger.info(f"{func.__name__} 执行时间: {end_time - start_time:.2f}秒")
            return result
        except Exception as e:
            end_time = time.time()
            logger.error(f"{func.__name__} 执行失败 (耗时: {end_time - start_time:.2f}秒): {str(e)}")
            raise
    return wrapper

@app.post("/api/v2/generate")
@timing_decorator
async def generate(request: UniversalVisualizationRequest):
    # 业务逻辑
    pass
```

## 🔍 6. API测试工具

### 6.1 curl命令详解
```bash
# 基础GET请求
curl http://localhost:8000/api/v2/templates

# 包含响应头
curl -I http://localhost:8000/api/v2/templates

# 只显示响应头
curl -s -o /dev/null -D - http://localhost:8000/api/v2/templates

# POST请求
curl -X POST "http://localhost:8000/api/v2/generate" \
  -H "Content-Type: application/json" \
  -d '{"prompt": "正弦函数"}'

# 详细信息
curl -v http://localhost:8000/api/v2/templates

# 保存响应到文件
curl -o response.json http://localhost:8000/api/v2/templates

# 设置超时
curl --max-time 10 http://localhost:8000/api/v2/templates
```

### 6.2 HTTPie (现代curl替代)
```bash
# 安装 (macOS)
brew install httpie

# GET请求
http GET localhost:8000/api/v2/templates

# POST请求
http POST localhost:8000/api/v2/generate \
  prompt="正弦函数" \
  template_id="default"

# 彩色输出
http --pretty=all GET localhost:8000/api/v2/templates
```

## 💻 7. IDE/编辑器配置

### 7.1 VS Code推荐插件
```json
// .vscode/extensions.json
{
  "recommendations": [
    "ms-python.python",
    "ms-python.black-formatter",
    "ms-python.flake8",
    "bradlc.vscode-tailwindcss",
    "esbenp.prettier-vscode",
    "ms-vscode.vscode-typescript-next",
    "vue.volar",
    "ms-vscode-remote.remote-containers"
  ]
}
```

### 7.2 VS Code设置
```json
// .vscode/settings.json
{
  "python.defaultInterpreterPath": "./venv/bin/python",
  "python.linting.enabled": true,
  "python.linting.flake8Enabled": true,
  "python.formatting.provider": "black",
  "editor.formatOnSave": true,
  "editor.codeActionsOnSave": {
    "source.organizeImports": true
  },
  "files.exclude": {
    "**/__pycache__": true,
    "**/*.pyc": true,
    "venv": true
  }
}
```

## 🚀 8. 自动化工具

### 8.1 Pre-commit钩子
```yaml
# .pre-commit-config.yaml
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.4.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files

  - repo: https://github.com/psf/black
    rev: 23.3.0
    hooks:
      - id: black

  - repo: https://github.com/pycqa/flake8
    rev: 6.0.0
    hooks:
      - id: flake8
```

### 8.2 GitHub Actions
```yaml
# .github/workflows/ci.yml
name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        pip install -r requirements.txt
        pip install pytest

    - name: Run tests
      run: pytest

    - name: Run linting
      run: |
        pip install black flake8
        black --check .
        flake8 .
```

## 📚 9. 学习资源

### 9.1 官方文档
- FastAPI: https://fastapi.tiangolo.com/
- Python: https://docs.python.org/
- Git: https://git-scm.com/doc
- Docker: https://docs.docker.com/

### 9.2 在线工具
- Python在线解释器: https://www.python.org/shell/
- JSON格式化: https://jsonformatter.org/
- Base64编码: https://www.base64encode.org/
- UUID生成器: https://www.uuidgenerator.net/

### 9.3 学习平台
- 实验楼: https://www.shiyanlou.com/
- 菜鸟教程: https://www.runoob.com/
- MDN Web Docs: https://developer.mozilla.org/

## 💡 使用技巧

### 1. 命令行技巧
```bash
# 历史搜索
ctrl + r

# 命令自动补全
tab

# 清屏
clear 或 ctrl + l

# 查找文件内容
grep "keyword" file.py

# 查找文件
find . -name "*.py"

# 查看进程
ps aux | grep python

# 端口占用
lsof -i :8000
```

### 2. 调试技巧
```python
# 使用断点调试
import pdb; pdb.set_trace()

# 使用print调试
print(f"调试信息: {variable}")

# 使用日志调试
import logging
logging.basicConfig(level=logging.DEBUG)
logger = logging.getLogger(__name__)
logger.debug(f"调试信息: {variable}")
```

### 3. 性能优化
```bash
# 分析Python代码性能
python -m cProfile main.py

# 内存使用分析
pip install memory-profiler
python -m memory_profiler main.py

# 代码复杂度分析
pip install radon
radon cc main.py -a
```

---

**掌握这些工具后，您的开发效率将大幅提升！** 🚀

记住：工具是手的延伸，善用工具能让您事半功倍！