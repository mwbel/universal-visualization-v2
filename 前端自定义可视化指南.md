# 🎨 前端自定义可视化完整指南

**日期**: 2025-11-05
**系统状态**: 所有服务运行正常 ✅
**后端API**: `http://localhost:8000` | **前端服务**: `http://localhost:8888`

## 🚀 前端入口方式

### 方式一：主应用入口 (推荐)
```
http://localhost:8888/main-app/index.html
```

### 方式二：智能可视化演示
```
http://localhost:8888/main-app/test/intelligent-viz-demo.html
```

### 方式三：高中数学可视化
```
http://localhost:8888/main-app/high-school/math/index.html
```

### 方式四：直接API调用
```
POST http://localhost:8000/resolve_or_generate
```

## 🎯 自定义可视化方法

### 方法一：通过主应用界面

**访问**: `http://localhost:8888/main-app/index.html`

**步骤**:
1. 打开主应用界面
2. 点击"演示功能"按钮或寻找可视化输入区域
3. 在文本框中输入你的可视化需求
4. 点击生成按钮

**支持的输入示例**:
```
# 概率分布类
正态分布 均值2 标准差1.5
二项分布 n=50 p=0.3
泊松分布 λ=5
均匀分布 -3到7
指数分布 λ=1.2
卡方分布 自由度8
t分布 df=10

# 自然语言描述
显示两个正态分布的对比
生成一个标准正态分布
创建一个泊松分布图
```

### 方法二：智能可视化演示页面

**访问**: `http://localhost:8888/main-app/test/intelligent-viz-demo.html`

**特色功能**:
- 🎯 **三种输入模式**: 简单文本、结构化参数、高级配置
- 📊 **实时预览**: 输入即时反馈
- 🔄 **重新生成**: 一键重新生成可视化
- 📱 **响应式设计**: 适配各种设备

**使用方法**:
1. 选择输入模式（简单/结构化/高级）
2. 输入你的可视化需求
3. 点击"生成可视化"按钮
4. 查看生成的交互式图表
5. 可以调整参数或重新生成

### 方法三：高中数学可视化模块

**访问**: `http://localhost:8888/main-app/high-school/math/index.html`

**特色功能**:
- 📚 **教育专用**: 专为数学学习设计
- 🎓 **互动学习**: 参数调节+实时反馈
- 📖 **知识讲解**: 包含数学概念说明
- 🧮 **计算验证**: 显示详细的统计计算

**支持的可视化**:
- 函数图像（一次、二次、指数、对数、三角函数）
- 概率分布（正态、二项、泊松、均匀、指数、卡方、t分布）
- 统计图表（直方图、散点图、箱线图）

### 方法四：直接API调用

**端点**: `POST http://localhost:8000/resolve_or_generate`

**请求格式**:
```json
{
  "prompt": "你的可视化描述",
  "vizType": "auto",
  "complexity": "中等",
  "params": {}
}
```

**JavaScript调用示例**:
```javascript
async function generateCustomVisualization(description) {
  try {
    const response = await fetch('http://localhost:8000/resolve_or_generate', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        prompt: description,
        vizType: 'auto',
        complexity: '中等',
        params: {}
      })
    });

    const result = await response.json();

    if (result.success && result.htmlContent) {
      // 在页面中显示可视化
      document.getElementById('viz-container').innerHTML = result.htmlContent;
      console.log('✅ 可视化生成成功');
    } else {
      console.error('❌ 生成失败:', result.message);
    }
  } catch (error) {
    console.error('❌ 请求失败:', error);
  }
}

// 使用示例
generateCustomVisualization('正态分布 均值3 标准差2');
```

## 🎨 自定义参数配置

### 基础参数配置

```json
{
  "prompt": "正态分布 均值5 标准差1.5",
  "vizType": "auto",
  "complexity": "中等",
  "params": {
    "width": 800,
    "height": 600,
    "theme": "light",
    "interactive": true
  }
}
```

### 高级参数配置

```json
{
  "prompt": "比较两个正态分布",
  "vizType": "comparison",
  "complexity": "高级",
  "params": {
    "distributions": [
      {"type": "normal", "mu": 0, "sigma": 1, "color": "#FF6B6B"},
      {"type": "normal", "mu": 2, "sigma": 1.5, "color": "#4ECDC4"}
    ],
    "xRange": [-10, 10],
    "yRange": [0, 0.5],
    "showLegend": true,
    "showGrid": true,
    "animation": true
  }
}
```

## 🔧 创建自定义可视化页面

### 步骤1: 创建HTML文件

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>我的自定义可视化</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        .input-area { margin: 20px 0; }
        .viz-container { margin: 20px 0; border: 1px solid #ddd; padding: 20px; }
        button { padding: 10px 20px; background: #007bff; color: white; border: none; cursor: pointer; }
        input { width: 300px; padding: 8px; margin: 5px; }
    </style>
</head>
<body>
    <h1>🎨 自定义可视化生成器</h1>

    <div class="input-area">
        <h3>描述你想要的可视化</h3>
        <input type="text" id="vizInput" placeholder="例如: 正态分布 均值0 标准差1" />
        <button onclick="generateVisualization()">生成可视化</button>
    </div>

    <div class="viz-container" id="vizContainer">
        <p>可视化内容将在这里显示...</p>
    </div>

    <script src="custom-viz.js"></script>
</body>
</html>
```

### 步骤2: 创建JavaScript文件

```javascript
// custom-viz.js
async function generateVisualization() {
    const input = document.getElementById('vizInput').value;
    const container = document.getElementById('vizContainer');

    if (!input.trim()) {
        alert('请输入可视化描述');
        return;
    }

    container.innerHTML = '<p>🔄 正在生成可视化...</p>';

    try {
        const response = await fetch('http://localhost:8000/resolve_or_generate', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
            body: JSON.stringify({
                prompt: input,
                vizType: 'auto',
                complexity: '中等',
                params: {}
            })
        });

        const result = await response.json();

        if (result.success && result.htmlContent) {
            container.innerHTML = result.htmlContent;
            console.log('✅ 可视化生成成功');
        } else {
            container.innerHTML = `<p>❌ 生成失败: ${result.message || '未知错误'}</p>`;
        }
    } catch (error) {
        container.innerHTML = `<p>❌ 网络错误: ${error.message}</p>`;
        console.error('请求失败:', error);
    }
}

// 支持回车键生成
document.getElementById('vizInput').addEventListener('keypress', function(e) {
    if (e.key === 'Enter') {
        generateVisualization();
    }
});

// 页面加载时生成一个默认示例
window.addEventListener('load', function() {
    document.getElementById('vizInput').value = '正态分布 均值0 标准差1';
    generateVisualization();
});
```

## 🎯 预设模板和示例

### 模板1: 概率分布对比器

```javascript
function compareDistributions() {
    const distributions = [
        '正态分布 均值0 标准差1',
        '正态分布 均值2 标准差1.5',
        't分布 自由度5'
    ];

    distributions.forEach((dist, index) => {
        setTimeout(() => {
            generateCustomVisualization(dist);
        }, index * 1000);
    });
}
```

### 模板2: 参数敏感性分析

```javascript
function analyzeParameterSensitivity() {
    const sigmas = [0.5, 1.0, 1.5, 2.0, 2.5];

    sigmas.forEach(sigma => {
        generateCustomVisualization(`正态分布 均值0 标准差${sigma}`);
    });
}
```

### 模板3: 教学演示序列

```javascript
function teachingDemo() {
    const sequence = [
        '标准正态分布',
        '二项分布 n=20 p=0.5',
        '泊松分布 λ=3',
        '均匀分布 0到1',
        '指数分布 λ=1'
    ];

    let index = 0;

    function showNext() {
        if (index < sequence.length) {
            generateCustomVisualization(sequence[index]);
            index++;
            setTimeout(showNext, 3000); // 每3秒切换
        }
    }

    showNext();
}
```

## 📱 移动端适配

### 响应式设计要点

```css
/* 移动端适配 */
@media (max-width: 768px) {
    .viz-container {
        width: 100%;
        height: auto;
        overflow-x: auto;
    }

    .controls {
        display: flex;
        flex-direction: column;
    }

    input {
        width: 100%;
        margin-bottom: 10px;
    }

    button {
        width: 100%;
        padding: 12px;
    }
}
```

## 🚨 错误处理和调试

### 常见错误解决方案

1. **CORS错误**: 确保后端服务运行在8000端口
2. **连接失败**: 检查后端服务状态
3. **生成失败**: 检查输入描述格式
4. **显示异常**: 检查HTML容器

### 调试工具

```javascript
// 添加调试日志
function debugVisualization() {
    console.log('开始调试可视化生成...');

    // 检查后端连接
    fetch('http://localhost:8000/health')
        .then(response => response.json())
        .then(data => console.log('后端状态:', data))
        .catch(error => console.error('后端连接失败:', error));
}

// 在控制台运行调试
debugVisualization();
```

## 🎉 进阶功能

### 1. 批量生成
```javascript
async function batchGenerate(descriptions) {
    for (const desc of descriptions) {
        await generateCustomVisualization(desc);
        await new Promise(resolve => setTimeout(resolve, 1000));
    }
}
```

### 2. 导出功能
```javascript
function exportVisualization() {
    const vizContent = document.getElementById('vizContainer').innerHTML;
    const blob = new Blob([vizContent], { type: 'text/html' });
    const url = URL.createObjectURL(blob);

    const a = document.createElement('a');
    a.href = url;
    a.download = 'visualization.html';
    a.click();
}
```

### 3. 分享功能
```javascript
function shareVisualization(description) {
    const shareUrl = `${window.location.origin}?prompt=${encodeURIComponent(description)}`;

    if (navigator.share) {
        navigator.share({
            title: '万物可视化',
            text: `查看我的可视化: ${description}`,
            url: shareUrl
        });
    } else {
        navigator.clipboard.writeText(shareUrl);
        alert('分享链接已复制到剪贴板');
    }
}
```

---

**🎊 现在你已经掌握了万物可视化系统的所有前端自定义方法！**

从简单的文本输入到复杂的参数配置，从单个可视化到批量生成，你可以根据需要选择最适合的方式来创建个性化的交互式可视化。